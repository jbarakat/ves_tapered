These input files were generated by solving the full set of ODEs.

The pressure and tensions are scaled relative to the bending
stress scale. In the output files generated using the reduced
set of ODEs in the high-Ca limit, they will be scaled relative
to the viscouse stress scale.
